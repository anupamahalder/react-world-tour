
const CountryData = (props) => {
    const {country, handleVistedCountry,handleVisitedFlags} = props;
    console.log(country, handleVistedCountry,handleVisitedFlags);
    return (
        <div>
        </div>
    );
};

export default CountryData;